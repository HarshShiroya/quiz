# This is a simple Python script
name = input("What is your name? ") # Get the user's name
age = int(input("How old are you? ")) # Get the user's age and convert it to an integer
print("Hello, " + name + "!") # Print a greeting message
print("You are " + str(age) + " years old.") # Print the user's age and convert it to a string
